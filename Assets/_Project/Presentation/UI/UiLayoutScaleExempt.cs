using UnityEngine;

namespace Line98.Presentation
{
    /// <summary>Marks a prefab subtree whose layout is authored independently of HUD scale passes.</summary>
    public sealed class UiLayoutScaleExempt : MonoBehaviour
    {
    }
}
